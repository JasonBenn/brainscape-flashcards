* On immutable.js docs, there are two problems:
  * Methods with periods aren't properly `replace`'d. See: List.of, Map.isMap, etc.
  * `padding-left: 2ch` doesn't appear to be working in code samples with indentation.